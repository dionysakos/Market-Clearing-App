import streamlit as st

st.title("Dashboard Overview")
st.caption("This legacy page remains in the repository for compatibility but is not used by navigation.")
st.info("Use the main app entrypoint (`app/app.py`) to access the professional dashboard with updated navigation and analytics views.")
